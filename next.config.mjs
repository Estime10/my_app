/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			'randomuser.me',
			'i.pinimg.com',
			'img.clerk.com',
			'firebasestorage.googleapis.com',
		],
	},
}

export default nextConfig;
